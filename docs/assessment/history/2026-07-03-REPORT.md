# PERSONAL CFO — Production Assessment

**Date:** 2026-07-03  ·  **Commit:** `9296d40` (main @ Phase 5b + deploy-swap)  ·  **App LOC:** ~9.0k  ·  **Tests:** 177 passed / 4 skipped (local + CI)

## VERDICT: PRODUCTION-READY — YES (Road A / single-user) · CONDITIONAL for multi-tenant

**Zero BLOCKER, zero SEV1.** The app is live, healthy, and every load-bearing security/lifecycle
invariant holds for its stated scope (single-user personal finance). It clears the Road-A finish line
and is safe to run for its owner today.

This pass was **deeper than the 2026-07-02 remediation-verification pass** and honestly surfaces an
**18-item SEV2 backlog** (the prior report's "3 SEV2/cosmetic" was optimistic). None gate personal
use, but several matter specifically for the **portfolio/resume-showcase goal** — a senior reviewer
reading this code would expect the money source-of-truth drift, the incomplete LLM token accounting,
the crash-on-truncation paths, and the silently-no-op disclaimer override to be fixed. For a genuine
**multi-tenant production** deployment the verdict is **CONDITIONAL**, gated on the deferred Road-B
block (tenant isolation) plus observability and load evidence (axes D/G + load test).

---

## Layer 0 — deterministic gates (from _machine.json)
| Gate | Baseline | Now | Status |
|---|---|---|---|
| ruff | 0 | **0** | ✅ |
| mypy (deps resolved) | 152 | **114 real errors** | ⚠️ permissive ceiling; ~67 are ORM/pydantic-settings false positives, rest genuine. Phase 5b added +3 |
| bandit | 0 / 0 | **0 / 0** | ✅ |
| pip-audit (`requirements.txt`) | 0 | **0** | ✅ (the `72` in `_machine.json` is the ambient Python env, not the pinned deps — script audits the wrong env) |
| coverage | floor 0.0 | **67.65%** | ⚠️ no meaningful floor set; ratchet the baseline to ~65% to catch drops |

> **Layer 0 caveats for the next run:** the script's `pip_audit` gate audits the *interpreter's*
> environment, not `requirements.txt` (real project vulns = **0**), and `mypy` run without the app's
> venv floods `import-not-found` (real type-error count with deps resolved = **114**, not 155).
> Consider a `sources.json` + a `pip-audit -r requirements.txt` tweak so the numbers are trustworthy
> unattended.

## Layer 1 — module register (ranked)

**Totals: 0 BLOCKER · 0 SEV1 · 18 SEV2 · ~25 cleanup.** Module verdicts: integrations **clean**;
routers / vault / agent / worker_scenarios_memory / _root_infra **NEEDS-WORK** (all SEV2, no blockers).

| Sev | Module | Location | Issue | Backed fix |
|---|---|---|---|---|
| SEV2 | vault | `wealth_position.py:42-46` | Year-versioned tax constants (`IRA 7000`, `HSA 4300`, `401k 23500`, `7% APR`) **re-hardcoded** despite `financial_snapshot.py` importing them from `agent/principles.py` — dual source of truth, silent drift at 2027 rollover (violates CLAUDE rule) | Delete locals; `from agent.principles import ROTH_IRA_LIMIT_2026, HSA_LIMIT_SINGLE_2026, …` |
| SEV2 | vault | `financial_snapshot.py:89-94` vs `wealth_position.py:204` vs `income_position.py:53` | "Sum Expense → monthly" duplicated 3× and **already diverged**: snapshot lacks the `Decimal("3000")` floor the ladders use → empty-Expense table shows `savings_rate=100%` while the ladder shows a ~$9k gap. Inconsistent user-facing numbers | Extract `sum_monthly_expenses(session) -> Decimal`, decide the fallback once, call from all three |
| SEV2 | vault | `financial_snapshot.py:167-209` | Nested money in `analysis_jsonb` cast to `float(...)` before storage (violates no-float-money) — and this payload is exactly what the LLM/UI reason over | Keep `Decimal` (EncryptedJSON already serializes Decimal→str); drop the `float()` casts |
| SEV2 | agent | `nodes/*.py` (analyzer/strategist/career/tax/coach) | Only the **Synthesizer**'s tokens reach `AgentState`/DB; the other 1–5 Anthropic calls per turn are undercounted (violates "token usage logged after every call") | Each node returns `tokens_in/out`; add `Annotated[int, operator.add]` reducers in `state.py`; persist the sum |
| SEV2 | agent | `nodes/{analyzer,strategist,career,tax_optimizer,synthesizer}.py` | Bare `next(b … if b.type=='tool_use')` → `StopIteration` → uncaught **500** when the model truncates at `max_tokens` (Coach already guards this; these 5 don't) | `next((…), None)` + handle `None`/`stop_reason=='max_tokens'` with a fallback or handled 502 |
| SEV2 | agent | `nodes/coach.py:44` | Coach's `principle` field is unconstrained `{"type":"string"}` (others pin the §4 registry enum); Coach *replaces* proposals so it can emit an off-registry key. Arena keys it's told to cite aren't in `PRINCIPLES` → `get_principle()` `KeyError` | Add `"enum": get_all_keys()` to the Coach tool; register arena keys in §4/principles.py first |
| SEV2 | agent | `nodes/tax_optimizer.py:103` | LLM call doesn't log token usage (every other node does) | Add `response.usage.input_tokens/.output_tokens` to the log line |
| SEV2 | routers | `imports.py:66-84` | Per-row **N+1** in CSV/OFX import: a `SELECT` + `flush()` + audit `INSERT` for every row → thousands of round-trips per statement | Pre-load existing hashes in one query into a set, filter in memory, `add_all()`, one summary audit row |
| SEV2 | routers | `vault.py:486` + `holdings.py:86` | The two endpoints that fan out to **paid external APIs** (RentCast, yfinance) have **no `@limiter.limit`**, while cheaper LLM routes do — a tight client loop burns quota | `@limiter.limit("6/hour")` (+ `request: Request`) on both refresh endpoints |
| SEV2 | routers | `holdings.py:93-112` + `crud.py:873` | `list_distinct_tickers` returns **every** row → batch refresh fetches each ticker's price N times; `total` counts rows not tickers | `select(distinct(Holdings.ticker))`, fetch once, update all rows for that ticker; rename |
| SEV2 | routers | `imports.py:23-30` | Unvalidated `account_id` query param → `IntegrityError` at COMMIT → unhandled **500** for a client-input error | `get_account` check → `404/422` before parsing |
| SEV2 | worker | `cron.py:41` + `digest.py:137` | Weekly digest **not idempotent**: `/digest/run-now` + cron (or a 2nd replica) send duplicate emails (in-process scheduler mitigates the cron-only case) | Gate `generate_and_send_digest()` on a `sent_log(year_week UNIQUE)` `INSERT … ON CONFLICT DO NOTHING`, or document the single-container assumption |
| SEV2 | worker | `cron.py:41-47,87-92` | Module-level `_http` httpx.AsyncClient **never closed**; `stop_scheduler()` leaks its pool every shutdown/test lifespan | `await _http.aclose()` in `stop_scheduler` |
| SEV2 | _root_infra | `disclaimer.py:18` | `get_disclaimer()` reads `os.environ["WEALTH_DISCLAIMER_TEXT"]`, but pydantic-settings loads `.env` into `Settings` **without** exporting to `os.environ` → the documented override **silently no-ops** | `return get_settings().wealth_disclaimer_text or DISCLAIMER`; drop the `os` import |
| SEV2 | _root_infra | `digest.py:131` | `smtplib.SMTP(host, port)` opened with **no timeout** → on a blackholed SMTP host the worker thread hangs forever and the `/run-now` request never returns | `smtplib.SMTP(host, port, timeout=30)` |
| SEV2 | integrations | `market_data.py:35` | `wait_for` bounds the coroutine but can't cancel the `to_thread` worker; `fast_info` sets no HTTP timeout → a hung Yahoo endpoint **leaks a thread** from the bounded default executor (could stall all `to_thread` work) *(needs-runtime-confirmation)* | Timeout-bearing `requests.Session` on the yfinance Ticker, or a module-level `asyncio.Semaphore` |
| SEV2 | integrations | `csv_import.py:113` | `ofxtools.Parser` on untrusted upload bytes with no XML-entity hardening → "billion laughs" DoS on the parse thread (bounded by 10MB cap + single owner) *(needs-runtime-confirmation)* | `defusedxml.defuse_stdlib()` before parse; pin `defusedxml` in requirements.txt |
| SEV2 | integrations | `market_data.py:1,58` | yfinance scrapes Yahoo, whose TOS bars redistribution/commercial use — load-bearing for the "showcase" framing and any future 2nd user | Docstring + DECISIONS/THREAT_MODEL note: personal-use-only; Alpha Vantage is the licensed path |

**Cleanup (~25, no behavior risk):** `str`-typed `CurrentUser` that's actually `User` (8 routers); HTMX
field-tuple duplication across ~18 vault handlers (DRY); no-op `cache_control` on sub-1024-token
prompts (agent); DRY LLM-node boilerplate; dead `financial_snapshot` state key; dead
`_WealthPositionContract`/`_IncomePositionContract` classes; truncated cadence factors in `_money.py`
(`4.333` vs `52/12`); missing `ix_decisions_status_decided_at` index; double `get_settings()` at
import; untyped `settings` params; broad `except Exception`; column-naming convention break
(`last_known_price` not `_encrypted`). Full detail in `docs/assessment/modules/*.md`.

**Documented-and-NOT-flagged (verified deliberate):** `Account.plaid_account_id` plaintext (empty;
Plaid deferred), `Transaction.description/notes` plaintext (hash is the sensitivity boundary),
`k401_match_capture_pct` NULL placeholder, unbounded `list_*` (pagination → 4b), no per-user `WHERE`
(single-user Road A). All other sensitive value columns **are** encrypted at the TypeDecorator boundary.

## Layer 2 — scale checklist (A–I)
| Axis | Status | Evidence |
|---|---|---|
| A Pool math | ⚠️ | `pool_size=5, max_overflow=5` = 10/proc, single replica vs PG 100 — safe by headroom; **no load evidence** |
| B Async loop hygiene | ✅ | Every blocking call (yfinance, RentCast, SMTP, CSV/OFX) offloaded via `to_thread` + timeout; verified across integrations + digest |
| C Job idempotency | ⚠️ | Digest not idempotent (SEV2) — in-process `coalesce`/`misfire_grace` mitigate cron; `/run-now`+cron can dup |
| D Tenant isolation | ⚠️ | **Expected ⚠️** — single-user Road A, no `tenant_id`; RLS/owner-filter is the Road-B headline (DECISIONS 2026-07-02) |
| E Backpressure | ⚠️ | Timeouts on Anthropic/yfinance/RentCast/digest-`wait_for`; **gap: SMTP has no timeout** (SEV2); no circuit breaker |
| F Rate limit/quota | ⚠️ | Redis-backed limiter on LLM routes ✅, but **paid external-API refresh endpoints are unthrottled** (SEV2); per-tenant quota = Road B |
| G Observability | ⚠️→❌ | Logging present but **no request/correlation id, no metrics (Prometheus/OTel), no error tracking (Sentry), no p50/p95/p99, no active alerting** (today's outage was noticed by chance). Token accounting also incomplete (SEV2). Biggest production gap |
| H Migration/index safety | ✅ | `CREATE INDEX CONCURRENTLY` + `lock_timeout` (Phase 4); one missing composite index (cleanup); **backup/PITR not restore-tested** (verify) |
| I Secrets/deletion | ✅ | MultiFernet rotation runbook exercised; `/docs` gated in prod; security headers; no secrets in logs; erasure endpoint = Road B (n/a single-user) |

## Diff vs previous report (2026-07-02, "YES (Road A)")
- **Fixed / newly landed:** Phase 5b shipped — `response_model` on every JSON endpoint (was deferred),
  security-headers middleware, slimmed `migrations/env.py`; deploy now auto-ensures VM swap
  (`[0/6]`, closes the ISSUE-2026-07-02-02 / -03-01 OOM-thrash root cause); tests 172→177; ruff still 0.
- **New (surfaced by a deeper pass, not new regressions):** the 18 SEV2s above. Most pre-existed; the
  prior remediation pass under-counted them as "3 SEV2/cosmetic." Notable honest corrections to prior
  ✅ claims: token logging is **not** "everywhere" (tax_optimizer + state undercount); rate limiting is
  **not** on all cost-bearing routes (external-API refresh unthrottled); the httpx "singleton" leaks on
  shutdown; observability is thinner than a ⚠️ implies.
- **Regressed:** mypy 152→155 raw (+3 from Phase 5b; true deps-resolved count 114). Minor.
- **Still deferred by design:** tenant isolation (Road B), list pagination (4b), plaid-encryption (5b,
  column empty), OFX defusedxml, multi-replica digest idempotency.

## Top 5 actions, in order (for the portfolio-showcase goal)
1. **Fix the money source-of-truth + float defects** (`vault`: SEV2 #1–3) — unify the tax constants and
   the expense-fallback helper, keep Decimal in `analysis_jsonb`. These produce *inconsistent
   user-facing numbers*, the worst look for a finance showcase.
2. **Complete LLM token accounting + guard truncation** (`agent`: SEV2 #4,5,7) — sum tokens across all
   nodes into state/DB, and replace the 5 bare `next(tool_use)` extractions with the Coach's guarded
   pattern (extract a `run_proposal_node` helper — fixes DRY + both bugs at once).
3. **Wire observability + alerting** (axis G) — a `HEALTHCHECK_PING_URL` dead-man's-switch (already
   in config, just unset), request-id logging, and Sentry. Directly closes the "how would I even know
   it's down" gap this session's outage exposed.
4. **Throttle the paid-API refresh endpoints + add SMTP/yfinance timeouts** (`routers`/`_root_infra`/
   `integrations`: SEV2 #4,15,18) — cheap defense against quota burn and hung threads.
5. **Fix the silently-no-op disclaimer override + the imports N+1** (`_root_infra` #17, `routers` #8) —
   a compliance-adjacent config bug and the one real DB-thrash path.

## Operational note
Prod host is the **Oracle VM `129.80.102.20`** (not the DigitalOcean `creatorclip-vm` — see
ISSUE-2026-07-03-01). OOM-thrash outage this session was recovered via `oci` reboot and permanently
mitigated by the deploy's `[0/6] Ensuring swap` step.

## Bottom line
**YES for Road A / personal use — live and safe today.** For the *portfolio showcase*, land the Top-5
(especially the money-correctness and LLM-hygiene items) to make it read unambiguously senior-grade.
For *multi-tenant production*, it's **CONDITIONAL** on the Road-B block (tenant isolation) + observability
+ load evidence.
