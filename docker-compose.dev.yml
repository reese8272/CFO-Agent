services:
  app:
    # Local dev overrides: mount source for hot-reload
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    volumes:
      - .:/app
    restart: "no"
